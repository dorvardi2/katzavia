@model Katzavia.Models.Category

@{
    ViewData["Title"] = "Details";
}
<head>
    <link rel="stylesheet" href="~/css/menu.css">
</head>
<h1>Details</h1>

<div>
    <h4>Category</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        @*<dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.myProducts)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.myProducts)
        </dd>*@
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.myProducts)
        </dt>
        @*@foreach (var item in Model.myProducts)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name) ,
                </td>
            </tr>
        }*@
        <ul>
              @foreach (var a in Model.myProducts)
                {
                <li>
                    <div>
                        @a.Name ,
                        @a.Price$
                    </div>
                </li>
                }
        </ul>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
